# React useContext Hook 101

This repo is my minimal useContext() try-out with Tailwind.

## How does it work?

A button toggles the className on components through a context provider between light and dark modes.
